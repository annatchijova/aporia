# Red-team L4 — deterministic relaxations

## Controles revisados

| Amenaza | Control | Estado |
|---|---|---|
| Mutar el snapshot factual al calcular un what-if | función pura + endpoint sin evento/write | cerrado por código; falta D1 real |
| Declarar feasible por sugerencia del modelo | no participa ningún LLM; usa `evaluateSnapshot` | cerrado |
| Tratar unknown como satisfecho | evaluator mantiene unknown; presupuesto desconocido no produce opción | cerrado por tests |
| Sugerir concesión para opción ya factible | baseline se calcula antes de generar opciones | cerrado por test |
| Explosión de opciones | máximo 8, constraints y candidates ya acotados | cerrado para el límite actual |
| Ranking no reproducible | policy version, enteros y tie-breaks por IDs | cerrado por test de orden |
| Revisión stale | endpoint exige expectedRevision | cerrado por código; falta integración D1 |
| Vender “mínimo absoluto” cuando sólo se prueba una constraint | policy documenta alcance y limitación | riesgo explícito, no oculto |

## Veredicto preliminar

L4 es coherente como cálculo acotado de concesiones de una sola constraint. No debe presentar sus opciones como una solución global óptima ni mutar el snapshot factual. Requiere revisión integrada posterior al despliegue para confirmar el comportamiento de conflicto D1.
